package lecture

import lecture.chapter1.*

object Lecture {

    object Chapter1 {

        val byOne: `1-1 스코프빌더` get() = `1-1 스코프빌더`
        val byTwo: `1-2 Job, 구조화된 동시성` get() = `1-2 Job, 구조화된 동시성`
        val byThree: `1-3 취소와 타임아웃` get() = `1-3 취소와 타임아웃`
        val byFour: `1-4 서스펜딩 함수` get() = `1-4 서스펜딩 함수`
        val byFive: `1-5 코루틴 디스패처` get() = `1-5 코루틴 디스패처`
        val bySix: `1-6 CEH와 슈퍼바이저 잡` get() = `1-6 CEH와 슈퍼바이저 잡`
    }
}